# WindLab Assets

Save the app icon as:

```text
assets/AppIcon.icns
```

Preferred icon page:

```text
https://macosicons.com/?icon=JS6Srjibi5
```

The package script also accepts a direct `.icns` URL:

```bash
WINDLAB_ICON_URL="https://example.com/AppIcon.icns" scripts/package_app.sh
```
